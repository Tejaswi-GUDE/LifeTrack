# Copy to .env before running `npm run seed` or the API server.
# Atlas connection string (or local mongod URI).
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/lifetrack
PORT=4000
NODE_ENV=development
